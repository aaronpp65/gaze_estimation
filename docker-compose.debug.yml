version: '3.4'

services:
  gazeestimation:
    image: gazeestimation
    build:
      context: .
      dockerfile: ./Dockerfile
    command: ["sh", "-c", "pip install debugpy -t /tmp && python /tmp/debugpy --wait-for-client --listen 0.0.0.0:5678 -m uvicorn api:app --host 0.0.0.0 --port 8080"]
    ports:
      - 8080:8080
      - 5678:5678
